# Database
DATABASE_URL=postgresql://user:password@localhost:5432/chatbot

# Auth
JWT_SECRET=your-secret-key-min-32-chars

# Internal eval QA (/eval/*) — use a distinct secret in production
EVAL_JWT_SECRET=your-eval-secret-min-32-chars

# Encryption key for storing client OpenAI keys
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Environment
ENVIRONMENT=development
LOCALIZATION_MODEL=gpt-4o-mini
OPENAI_USER_RETRY_MAX_ATTEMPTS=3
OPENAI_USER_RETRY_BUDGET_SECONDS=1.5
GAP_TRANSIENT_MAX_ATTEMPTS=5
GAP_BASE_DELAY_SECONDS=30.0
GAP_MAX_DELAY_SECONDS=1800.0
ALLOWED_HOSTS=localhost,127.0.0.1
WIDGET_MESSAGE_MAX_CHARS=4000
WIDGET_CHAT_PER_CLIENT_RATE=1000/minute

# Frontend URL (used in email links)
FRONTEND_URL=http://localhost:3000

# Email (Brevo)
BREVO_API_KEY=
EMAIL_FROM=no-reply@getchat9.live

# CORS (comma-separated list of allowed origins)
# Add all frontend domains: local dev + production
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://getchat9.live

# Observability (PostHog product metrics, Sentry exception tracking)
POSTHOG_API_KEY=
POSTHOG_HOST=https://us.i.posthog.com
SENTRY_DSN=
GIT_SHA=
PIPELINE_RELEASE=   # e.g. v1.3.0 — human-readable label; falls back to short GIT_SHA

# Note: No global OPENAI_API_KEY needed.
# Each client provides their own OpenAI key via the dashboard.

# Next.js frontend (frontend/.env.local or Vercel env — set before build)
# NEXT_PUBLIC_API_URL=https://api.example.com
# Public bot ID from dashboard embed snippet (ch_...), not the secret api_key:
# NEXT_PUBLIC_LANDING_DEMO_BOT_ID=ch_
# Dashboard support widget (Chat9 dogfood bot — bot public_id and its api_key):
# NEXT_PUBLIC_CHAT9_BOT_ID=ch_
# NEXT_PUBLIC_CHAT9_API_KEY=ck_
