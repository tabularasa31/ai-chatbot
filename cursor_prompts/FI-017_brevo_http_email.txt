SETUP:
cd ~/Projects/ai-chatbot
git checkout main
git pull origin main
git checkout -b feature/brevo-http-email

FEATURE:
FI-017 Use Brevo HTTP API for email instead of SMTP

GOAL:
Replace the current SMTP-based email sending with Brevo's HTTP API so that:
- verification emails are reliably sent from Railway (no SMTP port issues),
- interface stays the same: `send_email(to, subject, body)`,
- in development (no API key) we still log emails to console and do NOT fail signup.

======================================================================
CODE DISCIPLINE
======================================================================

Scope (you MAY modify):
- backend/core/config.py
- backend/email/service.py
- tests/test_auth_email_verification.py (to adjust mocks)

Do NOT touch:
- backend/models.py
- backend/auth/routes.py (logic of register/verify)
- backend/auth/middleware.py
- any other modules (clients, documents, embeddings, chat, search, etc.)
- migrations

If you think something outside Scope must be changed, STOP and describe it in a comment instead of editing code.

======================================================================
BACKEND
======================================================================

We will:
- add `BREVO_API_KEY` to settings,
- change `send_email` to:
  - if no `BREVO_API_KEY` or `EMAIL_FROM` → dev mode (log to console, no network),
  - else send `POST https://api.brevo.com/v3/smtp/email` with proper JSON payload and headers.

----------------------------------------------------------------------
1) Config: add BREVO_API_KEY
----------------------------------------------------------------------

File: `backend/core/config.py`

- Add new setting:

```python
class Settings(BaseSettings):
    ...
    EMAIL_FROM: str | None = None
    SMTP_HOST: str | None = None  # keep for now, but won't be used by Brevo HTTP
    SMTP_PORT: int | None = None
    SMTP_USER: str | None = None
    SMTP_PASSWORD: str | None = None
    FRONTEND_URL: str = "http://localhost:3000"

    BREVO_API_KEY: str | None = None
```

- We keep the SMTP vars for now (backwards compatibility), but new code will prefer BREVO_HTTP.

----------------------------------------------------------------------
2) Implement send_email via Brevo HTTP API
----------------------------------------------------------------------

File: `backend/email/service.py`

Current behavior:
- if SMTP_HOST/EMAIL_FROM not set → log dev email,
- else try smtplib.SMTP(...).

We want to switch to Brevo HTTP API:
- Docs: https://developers.brevo.com/reference/sendtransacemail
- Endpoint: `POST https://api.brevo.com/v3/smtp/email`
- Header: `api-key: <BREVO_API_KEY>`, `Content-Type: application/json`.
- Body shape (minimal):

```json
{
  "sender": { "email": "no-reply@getchat9.live" },
  "to": [ { "email": "user@example.com" } ],
  "subject": "...",
  "textContent": "..."
}
```

Implementation plan:

1) Use `httpx` or `requests`?
   - Prefer `httpx` if already used in the project; otherwise `requests`.
   - If neither is in requirements, you MAY add `httpx` to `backend/requirements.txt`.

2) Update `send_email`:

```python
from __future__ import annotations

import json
import logging
from typing import Optional

import httpx  # or requests

from backend.core.config import settings

logger = logging.getLogger(__name__)


def send_email(to: str, subject: str, body: str) -> None:
    """Send email via Brevo HTTP API.

    - If BREVO_API_KEY or EMAIL_FROM is not configured, log email in dev mode.
    - Network/API errors are logged but do NOT raise (to avoid breaking signup).
    """

    if not settings.BREVO_API_KEY or not settings.EMAIL_FROM:
        # Dev fallback: log email instead of sending
        logger.info("EMAIL DEV MODE: to=%s subject=%s body=%s", to, subject, body)
        return

    payload = {
        "sender": {"email": settings.EMAIL_FROM},
        "to": [{"email": to}],
        "subject": subject,
        "textContent": body,
    }

    try:
        resp = httpx.post(
            "https://api.brevo.com/v3/smtp/email",
            headers={
                "api-key": settings.BREVO_API_KEY,
                "Content-Type": "application/json",
            },
            json=payload,
            timeout=10.0,
        )
        if resp.status_code >= 400:
            logger.warning(
                "Brevo email send failed: status=%s body=%s",
                resp.status_code,
                resp.text,
            )
    except Exception as e:  # noqa: BLE001
        logger.warning("Failed to send email via Brevo: %s", e)
```

Notes:
- Use a reasonable timeout (e.g. 10s) to avoid hanging /auth/register.
- Do NOT raise exceptions on failure — we log and return.
- For now we only support plain text email (`textContent`), no HTML.

3) Remove direct usage of smtplib.
- You can keep the old code commented during refactor, but in final version we should not import/use `smtplib` at all.

----------------------------------------------------------------------
3) Tests adjustments
----------------------------------------------------------------------

File: `tests/test_auth_email_verification.py`

Currently, tests likely:
- monkeypatch `send_email` or rely on SMTP dev mode.

Update tests to avoid network calls:

- In tests where signup triggers email, monkeypatch `send_email` to a no-op:

```python
from backend.email import service as email_service


def test_signup_sets_verification_token(..., monkeypatch):
    calls = []

    def fake_send_email(to: str, subject: str, body: str) -> None:
        calls.append((to, subject, body))

    monkeypatch.setattr(email_service, "send_email", fake_send_email)

    # proceed with signup, assert token fields and that fake_send_email was called
```

- Ensure tests DO NOT depend on real HTTP requests to Brevo (no external network in tests).

If tests were previously checking for console dev logs, update them to assert that `fake_send_email` was called instead.

======================================================================
CHECKS
======================================================================

Backend:
- Add `httpx` to `backend/requirements.txt` if needed.
- `alembic upgrade head` (no DB changes expected).
- `pytest tests/test_auth_email_verification.py -v`.
- Optionally full test suite.

Manual:
- Set env vars on Railway:
  - `EMAIL_FROM=no-reply@getchat9.live`
  - `BREVO_API_KEY=<your Brevo API key>`
  - (SMTP_* can stay but are no longer used)
- Deploy.
- Register a new user with a real email address.
- Confirm that a verification email arrives and link `/verify?token=...` works.
